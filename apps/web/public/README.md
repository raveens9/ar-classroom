# Model Assets

Place prepared model files here for Module 3/4 handoff testing:

- `Letter_A.glb`
- `Letter_B.glb`
- `Letter_C.glb`

These files are referenced by ML API responses as:

- `https://localhost:3000/models/<model_file>`

For Module 4 animation playback, include at least one baked animation clip in each GLB.
Preferred clip name: `Idle` (fallback is first available clip).
